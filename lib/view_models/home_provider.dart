import 'package:flutter/material.dart';

class NavigationBarProvider with ChangeNotifier {
  String _selectedMenu = "";

  // get selectedCategory => _selectedMenu;

  // set setSelectedCategory(String menu) {
  //   _selectedMenu = menu;
  //   notifyListeners();
  // }
}
